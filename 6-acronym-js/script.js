"use strict";

/* 

const parse = (str) => {
  if (typeof str !== "string") return;

  return str
    .trim()
    .split(/[- ]/)
    .map((word) => word.match(/[a-zA-Z]/)?.pop())
    .join("")
    .toUpperCase();
};

console.log(parse("Something - I made up from thin air"));
console.log(parse("The Road _Not_ Taken"));

const x = "_3$Zhem";
console.log(x.match(/[a-zA-Z]/).pop());

console.log("Something - I made up from thin air".split(/[- ]/).join(""));

const parse2 = (str) => {
  return str.split(/\s|-|_/).map((word) => word[0].toUpperCase());
};

// console.log(parse2(`Something - I made up from thin air`));

console.log(`Something - I made up from thin air`.split(/\s|-|_/));
*/
